#pragma once
#include "Command.h"
class Restart : public Command
{/*
	sf::Texture tex = (*Pictures::getInstance().getTexture(Restart_b));
	m_texture = (*Pictures::getInstance().getTexture(Restart_b));
	sf::Sprite s1;
	s1.setTexture(t1);
	s1.setPosition(1030, 25);
	m_window.draw(s1);

	if (sf::Mouse::isButtonPressed(sf::Mouse::Left))
	{
		sf::Vector2f mouse = m_window.mapPixelToCoords(sf::Mouse::getPosition(m_window));
		sf::FloatRect bounds = s1.getGlobalBounds();
		if (bounds.contains(mouse))
		{
			resetLevel(m_window);
		}
	}*/
};